# flowctl-executors

Executor plugins for [flowctl](https://github.com/cvhariharan/flowctl).

## Plugins

- **http** — Make HTTP requests

## Build
Run `make`, this will compile all the plugins and place the binaries in a `plugins` directory.
